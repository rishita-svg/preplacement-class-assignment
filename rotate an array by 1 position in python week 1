def rotate_right_by_one(arr):
    if len(arr) == 0:
        return arr
    last = arr[-1]
    return [last] + arr[:-1]

arr = [1, 2, 3, 4, 5]
rotated = rotate_right_by_one(arr)
print("Right rotated:", rotated)

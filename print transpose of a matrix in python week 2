def transpose(matrix):
    n = len(matrix)
    result = [[0] * n for _ in range(n)]  # create empty n x n matrix
    for i in range(n):
        for j in range(n):
            result[i][j] = matrix[j][i]
    return result
